import sublime, sublime_plugin

# text_in_window
class TextInWindowCommand(sublime_plugin.TextCommand):
    def run(edit):
        print("TODO")
        # out_panel = self.view.window.get_output_panel("outpanel")
        # out_panel.set_read_only(False)
        # self.run()

        # edit = pt.begin_edit()
        # self.run_command("show_panel", { "panel": "output.outpanel" })
        # self.window.run_command("focus_group", {"group": 0})
        # self.window.run_command("move_to_group", {"group": 1})
